# Copiar a .env en la raíz del proyecto Expo (no subir secretos al repo)
# URL completa del backend (recomendado)
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.5:4000

# Si no usas EXPO_PUBLIC_API_BASE_URL, puerto del API (por defecto 4000)
# EXPO_PUBLIC_API_PORT=4000
